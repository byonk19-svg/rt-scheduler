# Design / implementation follow-ups

## Homepage / marketing shadows

- [x] **Luminous preview shell documented (2026-04-12):** `DESIGN.md` → _Shipped wiring_ describes `.teamwise-home-preview-shell` depth and `--home-*` tokens; no new `shadow-tw-*` row was required because depth stays in a dedicated CSS utility.

- [ ] **Future `shadow-tw-*` additions:** When you introduce a **new** `shadow-tw-*` class for marketing or product chrome, append a one-line row to the **Shadow map** table in `DESIGN.md` in the same PR.
